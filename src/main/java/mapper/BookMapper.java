package mapper;

import com.google.gson.internal.LinkedTreeMap;
import database.Book;
import dto.BookDto;

import java.util.ArrayList;
import java.util.List;

public class BookMapper {

//    public static List<Book> mapBookDtoToEntity(BookDto bookDto){

//        List<Book> result = new ArrayList<>();
//        for (String key : bookDto.getAuthors().keySet()) {
//           Book book = new Book();
//            book.setAuthors(bookDto.getAuthors(key));
//            book.setTitle(bookDto.getTitle());
//            book.setNumber_of_pages(bookDto.getNumber_of_pages());
//            book.setFirstSentence(bookDto.getFirstSentence());
//
//            result.add(book);
//        }
//
//        return result;
//    }


}
